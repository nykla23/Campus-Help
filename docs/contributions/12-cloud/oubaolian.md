# 云服务器部署贡献说明

**姓名**：欧宝莲  
**学号**：2312190401  
**日期**：2026-05-27  

## 我完成的工作

- 选择阿里云 ECS 免费试用实例（Ubuntu 22.04）
- 配置安全组规则，开放 22（SSH）、3000（API）端口
- 在服务器上安装 Node.js 20、MySQL、pm2
- 上传后端代码，安装依赖，配置 `.env` 环境变量
- 使用 pm2 启动并守护 Node.js 应用，设置开机自启
- 测试 API 可访问性：`curl http://112.124.21.214:3000/health`
- 编写部署文档 `docs/deployment.md`
- 完成github actions自动部署配置

## 部署配置

- **部署方式**：手动 SSH + 命令行
- **进程管理**：pm2
- **环境变量**：通过 `.env` 文件管理

## 遇到的问题和解决

1. **问题**：git clone 失败，网络超时  
   **解决**：改用本地打包后 scp 上传代码。

2. **问题**：MySQL 导入 SQL 文件报 ASCII '\0' 错误  
   **解决**：暂时未完全解决，计划手动建表。

3. **问题**：安全组配置后仍无法访问 3000 端口  
   **解决**：检查了系统防火墙 `ufw`，确认已放行。

## 在线地址

[http://112.124.21.214:3000/health](http://112.124.21.214:3000/health)

## 心得体会

通过本次部署实践，我掌握了云服务器的基本操作、Node.js 应用的生产环境部署、pm2 进程管理、安全组配置等技能。遇到网络问题时学会了使用 scp 替代 git clone 上传代码，提升了对生产环境部署的理解。